# pipgate - Secure Python Package Installer

## Overview

pipgate is a security-enhanced package manager that audits dependencies before installation.

## Usage

```bash
python -m pipgate install <package>
```
